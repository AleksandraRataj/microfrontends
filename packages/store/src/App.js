import React from "react";
import ReactDOM from "react-dom";

const App = () => (
    <div>
        <h1>Redux store App</h1>
    </div>
);

ReactDOM.render(<App/>, document.getElementById("store-dev-root"));